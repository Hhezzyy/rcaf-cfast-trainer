[tool.pytest.ini_options]
    testpaths = ["tests"]
    addopts = "-q"
    cache_dir = ".pytest_cache_local"

[tool.ruff]
    line-length = 100
    target-version = "py313"

[tool.ruff.lint]
    select = ["E", "F", "I", "B", "UP"]